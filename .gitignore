node_modules/
dist/
build/
.cache/
.env*
!.env.example
!.env.production
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
__pycache__/
*.py[cod]
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Private supplier catalog; contains wholesale pricing and is research-only.
public/POR MAYOR P3 17 DE AGOSTO_260817_.pdf

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
